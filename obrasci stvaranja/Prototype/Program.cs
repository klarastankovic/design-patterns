using Prototype.Color;
using Prototype.DatasetLV3;

namespace Prototype
{
    internal class Program
    {
        static void Main(string[] args)
        {
            /*
            ColorTester.Test();
            Color.ColorPalette palette = new ColorPalette();
            palette["White"] = new Color.Color(0,0,0);
            Color.ColorPrototype white = palette["White"];
            Console.WriteLine(white);
            Console.WriteLine(palette["White"]);
            Console.WriteLine(palette["White"].Equals(white));
            */
        }
    }
}
